spring:
  datasource:
    name: book
    url: jdbc:mysql://localhost:3306/book?ServerTimezone=UTC
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
